# SSO Configuration - Company Login (OIDC)
# Provider options: 'google' or 'oidc'
VITE_SSO_PROVIDER=oidc

# OAuth Client ID (required)
# Get this from your company's identity provider (IT team)
# Format varies by provider
VITE_SSO_CLIENT_ID=

# OIDC Authority URL (required for company SSO)
# This is your company's identity provider endpoint
# Examples:
#   Azure AD: https://login.microsoftonline.com/{tenant-id}
#   Okta: https://{your-domain}.okta.com
#   Auth0: https://{your-domain}.auth0.com
#   Generic OIDC: https://your-identity-provider.com
VITE_SSO_AUTHORITY=

# OAuth Client Secret (optional, for token refresh)
# Get this from your company's identity provider
# Note: In production, this should be handled server-side for security
VITE_SSO_CLIENT_SECRET=

# OAuth Redirect URI (required)
# Update this to match your application URL
# For local development:
VITE_SSO_REDIRECT_URI=http://localhost:5173/auth/callback
# For production, update to: https://your-domain.com/auth/callback

# OAuth Scopes (optional, defaults to openid profile email)
# Common scopes: openid, profile, email, offline_access
# Your IT team can provide the required scopes
VITE_SSO_SCOPES=openid,profile,email,offline_access

# API Configuration (if needed)
# VITE_API_BASE_URL=http://localhost:3000
